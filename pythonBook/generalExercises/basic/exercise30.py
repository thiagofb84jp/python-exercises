"""
30. Write a Python program that will accept the
    base and height of a triangle and compute the area.
"""

base = int(input("Input the base: "))
height = int(input("Input the height: "))

area = base * height / 2

print(f"Area = {area:2.2f}")
